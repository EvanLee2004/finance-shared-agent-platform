# Linux deploy notes

- Independent port from 利润看板 (:80).
- OpenCode: pin version; one-click update with checksum + rollback.
- Skills remote: prefer Gitee when GitHub is slow.
- Developer Mac without proxy: use Gitee remotes; OpenCode updates run on the deploy host.
