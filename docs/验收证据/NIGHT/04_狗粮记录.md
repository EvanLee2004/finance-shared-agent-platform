# NIGHT 狗粮记录

## 路径

1. 按 README 起 API + 前端；设置 `FSA_BOOTSTRAP_*` 与可选 `FSA_SKILLS_ROOT`
2. 浏览器登录 admin
3. 工作台确认用户 / health / OC 状态
4. 管理员同步 Skills → 技能页出现 catalog 项
5. 打开对话：空态可读；模型列表走 OC API
6. 登出

## 阻塞与修复

| 问题 | 处理 |
|------|------|
| 无 skills 时仅静态说明 | 已实现本地 catalog 同步 + 空态文案指引 `FSA_SKILLS_ROOT` |
| 模型列表依赖 OC provider | 诚实空态；零硬编码白名单（见 test_oc_models） |
| 用户隔离 | test_chat_oc isolation 404 |

## 门禁

```bash
sh tests/run_verify.sh; echo EXIT:$?
# 期望 PASSED EXIT:0（本波 30 pytest）
```
