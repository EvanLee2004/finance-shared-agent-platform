# NIGHT 浏览器证据 · 点了→看到

| 项 | 值 |
|----|-----|
| 日期 | 2026-08-06 |
| 分支 | `feature/fsa-night-all-in-one` |
| 环境 | API `18000` · FE `5173` · 可选 OC `4096` · `FSA_SKILLS_ROOT`=本地 finance-shared-skills |
| 账号 | `admin` / `Phase0Demo1!` |
| 工具 | Chrome DevTools MCP |

## 步骤

| # | 点了 | 看到 |
|---|------|------|
| 1 | 打开 `/login` | 单表单登录；演示账号 admin/Phase0Demo1!；顶栏主题 Neon |
| 2 | 登录 | URL `/`；**工作台**；用户 admin/admin；Health **ok**；DB 可用；OC **在线** 1.18.3；入口：智能对话 / 本机能力 / 技能目录 |
| 3 | 技能目录 | URL `/skills`；「同步 Skills 仓」；16 项 published 可运行（ar-hexiao-daily…xlsx）；说明 published∩授权 |
| 4 | 智能对话 | URL `/chats`；空态「暂无会话，点新建」；模型选择器文案「来自 OpenCode」；无 provider 时「无可用模型」诚实提示（非硬编码名单） |
| 5 | 主题按钮 | 顶栏可切换 Light/Neon（localStorage `fsa_theme`） |

## API 狗粮（同环境）

- `GET /health`：db.ok；opencode.optional；skills_repo.ok + tip 真实
- `POST /auth/login` admin → 200
- `POST /admin/skills/sync` → upserted=16
- `GET /skills?scope=runnable` → 16 项 admin 可跑

## 结论

主路径可跟做：登录→工作台→技能→对话；无 OC 时中台仍可登录/工作台/技能浏览。
