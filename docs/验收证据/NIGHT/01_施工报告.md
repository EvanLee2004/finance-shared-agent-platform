# NIGHT 一夜全包 · 施工报告（中文）

| 项 | 值 |
|----|-----|
| worktree | `fsa-night-w0-stabilize` |
| 分支 | `feature/fsa-night-all-in-one` |
| 角色 | EXECUTOR · **REVIEW_READY** |
| 日期 | 2026-08-06 |
| tip SSOT | `git rev-parse HEAD`（见 `02_候选tip.txt`） |

## 交付摘要

### G0 总控
`docs/验收证据/NIGHT/00_总控勾选.md` 跟踪 G0–G8，抗 compact。

### G1 门禁与账号
- `run_verify` 双跑 **PASSED / EXIT:0**（30 pytest + ruff + frontend build）
- README：`admin` / `Phase0Demo1!` · 端口 5173/18000/4096 · `FSA_SKILLS_ROOT` 说明

### G2 工作台 + OC 生命周期
- 工作台：用户 / health / OC 状态 / 入口卡片（非空白）
- OC 启用：`enable-guide` 需登录；仅用户确认后展示可复制 install/upgrade/serve（不静默安装）

### G3 聊天 1↔1 + 人话错误
- 1 中台 chat ↔ 1 OC session；隔离测试 userB 读 A → 404
- OC down 发送 → `oc_unavailable` 503 人话；extract_assistant_text 错误体可读

### G4 模型选择器
- `GET /opencode/models` → `OcClient.list_models` 仅 OC API；前端 select 无硬编码名单
- 切换时 localStorage 记忆；send 携带 providerID/modelID（test_oc_models）

### G5 Skills 同步
- `adapters/skills_repo.py`：解析 catalog.yaml；`FSA_SKILLS_ROOT` / 默认探测
- `POST /admin/skills/sync`：入库 published；本机狗粮 upserted=16（真实 Gitee 克隆）

### G6 授权隔离审计
- runnable = published ∩ grant（admin 全 published）
- `PUT /admin/grants`；`GET /admin/audit`；非 admin 403
- 测试：`tests/test_skills_grants.py` 驱动真实 HTTP

### G7 UX 主题空态
- light + neon（`fsa_theme`）；技能/对话空态中文说明

### G8 狗粮与证据
- `03_浏览器点了看到.md` · `04_狗粮记录.md` · 门禁绿

## 未做（禁令 / 非目标）

- push / SSH / 公司机部署 / 利润看板
- SSE 流式；完整审批流 UI；向 Gitee 推私有 skill

## 钉死 tip

```bash
git rev-parse HEAD
```

完整 40 位输出 = 下一波 worktree-ref。禁止把文件内历史 hash 当作当前 tip。
