# W7 浏览器走查 · 点了→看到

| 项 | 值 |
|----|-----|
| 日期 | 2026-08-06 |
| 分支 | `feature/fsa-night-all-in-one` |
| 账号 | admin / Phase0Demo1! |
| 工具 | Chrome DevTools MCP · Vite 5173 · API 18000 |
| 备注 | 本跑次 API 设 `FSA_OPENCODE_BASE_URL=http://127.0.0.1:1` 以测 OC 离线人话路径 |

## 路径

| # | 点了 | 看到 |
|---|------|------|
| 1 | 打开 `/login` | 顶栏「登录」；Element 表单用户名/密码；主按钮「登录」；演示账号克制 + 填入 |
| 2 | 登录 | 「登录成功」Message；URL `/`；导航 **工作台 / 智能对话 / 技能目录** |
| 3 | 工作台 | 用户 admin；中台**正常**；OC **离线** 白话「离线时仍可登录…」；「启用 OpenCode」；≥3 CTA |
| 4 | 主题 Light/Neon | 顶栏按钮切换；整页主题一致 |
| 5 | 智能对话 | 页标题「智能对话」；会话列表 **先 loading 后** 列表/空态（无空闪误导）；模型条「OpenCode 离线」中文 |
| 6 | 选会话 → 输入 → 发送（OC 离线） | 中文错误：**「OpenCode 未就绪。请在工作台启用 OpenCode 后再发送消息。」**；按钮 **「重试发送」** + **「去启用 OpenCode」**（非英文裸 Error） |
| 7 | 技能目录 | 列表或同步引导；授权白话 |
| 8 | 工作台 → **登出** | Message「已登出」；URL `/login`；顶栏回到「登录」无业务导航 |
| 9 | （结构） | 无 `alert(`/`confirm(`；listError 有「重试」 |

## API 对照（同 OC 离线环境）

```text
POST /auth/login → 200
POST /chats → 200
POST /chats/{id}/messages → 503 code=oc_unavailable 中文 message
POST /auth/logout → 200
GET  /me（logout 后）→ 401
```

证据文件：`{SCRATCH}/w7_send_oc_down.json` · `w7_logout.json` · `w7_browser.txt`

## 不合格句式自检

| 项 | 结果 |
|----|------|
| 眯眼字 | 未命中 |
| 空白首页 | 未命中 |
| 英文裸错误 | 未命中（OC 离线中文 + 可重试） |
| alert | 未命中 |
| 主题半残 | 未命中 |
| 列表加载闪空 | 已修：listLoading 优先于 empty |
