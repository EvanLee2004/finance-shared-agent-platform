# W10 浏览器走查 · 点了→看到

| 项 | 值 |
|----|-----|
| 日期 | 2026-08-06 |
| 账号 | admin / Phase0Demo1! |
| 环境 | API 18000 · FE 5173 · skills 已同步 |
| tip 产品 feat | `d641b25c5c1570ea356608382a2be7a391208c9c` |

## 使用者主路径

| # | 点了 | 看到 |
|---|------|------|
| 1 | 登录 | 进工作台；导航含 工作台/对话/技能/**管理** |
| 2 | 工作台「我的概况」 | **已发布技能总数 16** · **我可运行 16** · 我的会话 0 |
| 3 | 最近会话 | 空态中文引导；有会话时标题可点 → `/chats?id=<chatId>` 并选中该会话 |
| 4 | 智能对话 | 可新建；模型/发送依赖 OC |
| 5 | 技能目录 | 可浏览已发布 |
| 6 | 登出 | Message「已登出」；URL `/login` |

## 管理员路径

| # | 点了 | 看到 |
|---|------|------|
| 1 | 导航「管理」 | `/admin` |
| 2 | 授予运行权（角色 user） | Toast **已授予运行权**；审计表 `skills.grant` |
| 3 | 审计日志 | 可见 login / sync / grant |

## 失败恢复（本波补录）

| # | 点了 | 看到 |
|---|------|------|
| F1 | 登录页错误密码（或 API 等价） | 401 + 中文「用户名或密码错误」；表单仍可改密重试 · 自动化：`tests/test_auth.py::test_login_failed_uniform_401` |
| F2 | OC 不可用时发送消息 | HTTP 503 `code=oc_unavailable` 中文「OpenCode 未就绪…」；UI「重试发送」「去启用 OpenCode」 · 自动化：`tests/test_chat_oc.py::test_send_when_oc_down_returns_oc_unavailable` · W7 浏览器亦实测离线发送人话 |

## API

- `GET /dashboard/stats` → skill_total/runnable/chat_count  
- `PUT /admin/grants` · `GET /admin/audit` 由 UI 驱动  
