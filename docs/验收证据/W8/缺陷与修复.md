# W8 缺陷与修复账

| ID | 严重 | 复现 | 修复 | 回归 |
|----|------|------|------|------|
| W8-P1-001 | P1 | `POST /auth/login` body `{}` → 422 仅 FastAPI `detail` 数组，无 `code` | `RequestValidationError` → `{code:validation_error,message:中文…}` | `test_validation_error_envelope` |
| W8-P0-none | — | 鉴权/隔离/OC-down 主路径已有绿测，本波未发现新 P0 | — | `test_api_matrix` 全表 |
| W8-info-001 | info | 设计清单 Phase2 多端点未实现 | 矩阵 B 节标 unimpl·not-in-wave，不假实现 | 矩阵文档 |

## 安全抽测

| 项 | 结果 |
|----|------|
| 未登录受保护路由 | 401 `auth_required`（矩阵） |
| 跨用户读 chat | 404 `not_found` |
| 非 admin 写同步/授权 | 403 `forbidden` |
| 密码/token 日志 | 未新增打印 |
