# 主人早上打开看什么（W11 · 15 行）

1. 分支 `feature/fsa-night-all-in-one`，**未 push**；tip 用 `git rev-parse HEAD`。  
2. 一夜队列 W0→W11 收口：主路径可演示给部门。  
3. 启动：README 两终端 API:18000 + FE:5173；演示账号点「填入演示账号」。  
4. 登录→工作台：技能总数/可运行/会话数字 + OC 状态 + 入口卡片。  
5. 顶栏：工作台/对话/技能/**管理（仅 admin）** + 用户名 + **登出** + 浅色/暗色。  
6. 智能对话：模型来自本机 OC（无白名单）；离线可建会话看历史。  
7. 技能目录：published ∩ 授权；本机同步 16 项可浏览。  
8. 管理：授权 + 审计 + Skills 同步三件套真可用。  
9. 工作台可**修改密码**（成功后重新登录）。  
10. 安全：Cookie HttpOnly；登录不预填密；非 admin 不见管理入口。  
11. 边界（故意没做）：开户列表、附件、创作者审批、Gitee push。  
12. 证据：`docs/验收证据/W11/**`（缺陷清单 Open 中高=0、终态狗粮、反思报告）。  
13. 门禁：`sh tests/run_verify.sh` EXIT:0×2。  
14. DIRECTOR 验收即可；**禁止** push/SSH/公司部署/碰利润看板。  
15. 若要上线：先 RELEASER 清单 + 改演示口令 + 配 FSA_SKILLS_ROOT。  
