# W9 架构差距报告

对照：HLD 02 · 详设 04 · softeng 10 · 安全 06 · 真实 `backend/` `frontend/`

---

### W9-P0-01 · OC 单点出站

| 字段 | 内容 |
|------|------|
| **现状** | `OcClient` 在 `adapters/oc_client.py`；本波前 `api/v1/chats|health|system` 直接 import |
| **目标态** | 路由只经 `services/oc_service`；HTTP 仅 oc_client |
| **用户影响** | OC 挂不应拖垮登录；改聊天适配器不波及全 API |
| **是否拍板** | 是（概要：浏览器不直连 OC；唯一 adapter） |
| **优先级** | P0 |
| **本波动作** | **已修**：`oc_service` 门面；api 去掉 OcClient import |
| **证据** | `tests/test_arch_guards.py` · `test_api_matrix` · 主路径测绿 |

### W9-P0-02 · 鉴权在服务端

| 字段 | 内容 |
|------|------|
| **现状** | Cookie `fsa_sid` + deps.get_current_user；grant/admin 在 skill_service |
| **目标态** | 前端 isAdmin 仅 UI；写操作后端 403 |
| **用户影响** | 防越权装正常 |
| **是否拍板** | 是（安全设计） |
| **优先级** | P0 |
| **本波动作** | **已修/确认**：无回退；矩阵测 401/403 |
| **证据** | `test_api_matrix` unauth + forbidden |

### W9-P0-03 · 会话隔离

| 字段 | 内容 |
|------|------|
| **现状** | chat_service 按 user_id 硬过滤，越权 404 |
| **目标态** | 保持；测试不可删 |
| **用户影响** | 防串会话 |
| **是否拍板** | 是 |
| **优先级** | P0 |
| **本波动作** | **已修/确认** 不回退 |
| **证据** | `test_chat_oc` isolation · matrix |

### W9-P0-04 · 密钥不进仓/日志

| 字段 | 内容 |
|------|------|
| **现状** | 密码 Argon2；session SHA-256；无新增 print token |
| **目标态** | 继续 |
| **用户影响** | 防泄露 |
| **是否拍板** | 是 |
| **优先级** | P0 |
| **本波动作** | **确认**；run_verify secrets 检查 |
| **证据** | run_verify secrets OK |

### W9-P1-01 · 路由依赖 adapter

| 字段 | 内容 |
|------|------|
| **现状（改前）** | chats/health/system 直接 new OcClient |
| **目标态** | api → service → adapter |
| **用户影响** | 可维护性；改一处 OC 行为 |
| **是否拍板** | 规范要求，非产品拍板例外 |
| **优先级** | P1 |
| **本波动作** | **已修** |
| **证据** | oc_service + arch_guards |

### W9-P1-02 · 审计/可见性字符串散落

| 字段 | 内容 |
|------|------|
| **现状** | 部分 audit action 字面量 |
| **目标态** | domain.enums 单点 |
| **用户影响** | 修一漏二 |
| **是否拍板** | 规范 |
| **优先级** | P1 |
| **本波动作** | **已修** AUDIT_* / VIS_* |
| **证据** | enums.py · skill/chat_service |

### W9-P1-03 · 设计清单未实现端点

| 字段 | 内容 |
|------|------|
| **现状** | users CRUD、jobs、审批等无实现（W8 矩阵 later） |
| **目标态** | 按波次补 |
| **用户影响** | 管理端未齐，主路径可用 |
| **是否拍板** | 分期实现（设计 Phase 标注） |
| **优先级** | P2 / 拍板如此 |
| **本波动作** | **later** |
| **证据** | W8/01_接口矩阵 B 节 |

### W9-P2-01 · 前端 isAdmin 展示

| 字段 | 内容 |
|------|------|
| **现状** | SkillsView `me.role===admin` 控制同步按钮 |
| **目标态** | 仅 UX；后端仍 403 |
| **用户影响** | 可接受 |
| **是否拍板** | 是（前端不算真相） |
| **优先级** | P2 |
| **本波动作** | **later**（文档声明即可） |
| **证据** | test_api_matrix forbidden |

---

## P0 汇总

本波结束 **开放 P0 = 0**。
