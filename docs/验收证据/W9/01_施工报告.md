# W9 施工报告 · 架构体检与对齐（中文）

| 项 | 值 |
|----|-----|
| 分支 | `feature/fsa-night-all-in-one` |
| 状态 | **REVIEW_READY** |
| tip SSOT | `git rev-parse HEAD` |
| 基线 | W8 `33559ac…` |
| 日期 | 2026-08-06 |

## 代码变更

1. **新增** `services/oc_service.py`：路由/聊天默认经此构造 OC，禁止 api 直连 adapter  
2. **api/v1** `chats|health|system`：去掉 `from adapters.oc_client`  
3. **domain/enums**：`AUDIT_*` / `VIS_*` / principal 单点；chat/skill_service 使用  
4. **守卫** `tests/test_arch_guards.py`：api 禁 import oc_client；httpx+OC 路径仅 oc_client；services 禁 import api；前端禁 OC URL  

## 验证

- pytest 全量含 arch_guards / matrix / isolation（46）  
- `run_verify` **PASSED / EXIT:0 ×2**（ruff 修 import 后重跑；证据 `{SCRATCH}/w9_run_verify.log`）  
- tip 与 `{SCRATCH}/w9_git.txt` 一致：`f040db049bad8ac6cea42fce698995f2960ce87c`  

## 文档

- `01_模块地图.md` · `架构差距报告.md` · `后续架构goal草稿.md`  
- README 架构小节（现状=代码）  

## 未做

- push/SSH/部署/看板  
- 大搬家、换框架、补全 Phase2 产品面（见草稿）  

## tip

```bash
git rev-parse HEAD
# f040db049bad8ac6cea42fce698995f2960ce87c
```
