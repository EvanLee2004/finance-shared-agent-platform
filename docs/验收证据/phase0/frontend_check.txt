# Frontend structural / build check (Phase0)

Date: 2026-08-05

## Static structure

- frontend/package.json — Vue3 + Vite + vue-router
- frontend/vite.config.js — proxy `/api` → `http://127.0.0.1:18000`
- frontend/src/views/LoginView.vue — route `/login`
- frontend/src/views/HomeView.vue — shows username + role; logout button
- frontend/src/router/index.js — `/login`, `/` Home; no chat routes
- frontend/src/api/client.js — only `/api/v1/*` (no OpenCode URL)

## Build

`npm run build` inside `frontend/` succeeded via `tests/run_verify.sh`
(see scratch run_verify_*.log). Product dist/ is gitignored.

## Hand-test notes (no real customer data)

1. Start backend: `uvicorn app.main:app --host 127.0.0.1 --port 18000` with bootstrap admin env.
2. Start frontend: `cd frontend && npm run dev`.
3. Open `/login`, sign in, Home shows username/role, logout returns to login.
4. No chat UI present.

Optional visual: not attached (text evidence sufficient for Phase0 gate).
